# judge-loop decisions — 横断(cross-cutting)

append-only。新しいエントリは末尾に追記する。

## 2026-06-23 — _dev/ 開発ガバナンス層を新設
- 決定: judge-loop に `_dev/`(TODO / STATUS / decisions / archive)を新設。global CLAUDE.md の四役ガバナンス準拠。
- 理由: スキルを磨いていくのに、SKILL.md を「なぜこの形にしたか」を残しながら回す台帳が無かった。judge-loop 自身が説く G6(却下と裁定を台帳へ)を自分の開発に適用する。
- `docs/` との区別: `docs/` は判定ドメインの一次資料(方法論=WHY 台帳・あかつき引き継ぎ)。`_dev/decisions` はスキルの形についての meta-ADR。前者を後者へ複写しない。
- 兄弟スキル(ai-wiki/pdf-to-md/xlsx-router)は `_dev/` を開発ワークエリアにしている。慣習に合わせフォルダ名は `_dev/`。中身は四役で統一。

## 2026-06-23 — judge-loop の確定済み形(起点エントリ・git に接地)
過去コミットの why をここに集約し、蒸し返しを防ぐ。詳細根拠は docs/方法論。

- 薄いオーケストレータである(bc62dfb): 判定の中身は指示しない。フェーズ検出・ゲート強制・grill-hook 発火・sub-skill routing だけを足す。中身を持つと1ドメイン(あかつき)を継承して狭まる(G1 の自己適用)。
- ゲート vs 選択の二分が背骨(方法論 M1): G1–G9 は always-on 非交渉(過ちから帰納した不変条件)。cardinality・単位粒度・GT機構・judge モデル・安全側・precision/recall は焼き込まず use-time の grill-hook で決める。全決定を選択にすると梃子ゼロ、全部ゲートにすると継承で狭まる。
- composition(再実装しない): grill-me / gemma-prompt / review-server / claude-md / task-handoff へ routing。judge-loop が自前で持つ唯一のコードは K-run/flutter/分布diff ハーネスのみ。
- run-ledger = 第5記録型(9d49b54): decisions(ADR)/gt(verdict)/spike(単位反応)/outputs(機械判定) とは別。drift をどの版変更に帰属させるかを GT側version と run側version の両方で閉じる=幻のリグレッション殺し。
- decisions/archive 全読の蒸留を折り込み済(6d1c15e): N1 verdict 状態 taxonomy → P1 / N2 基準ルーティング → P4 / N3 中立ファクト層 → Premises / N4 決定論錨り → G8 / N5/N6/S1/S3/S5 → Cores 他。

- 出自リスク(留保): 本標準は n=1(あかつき)由来。各ゲートは「複数ドメインで成り立つ」ことで初めてゲートを名乗れる(帰納)。別ドメインで当てて overfit を検出するまで、どのゲートも「あかつき一事例の選択」に転落しうる。TODO Deferred に名指し。

## 2026-06-23 — SKILL.md ⇔ docs/方法論 整合チェック
- 実施: 両ファイルを全文 jibun-de 読みで照合(方法論→SKILL の取りこぼし / SKILL→方法論 の勝手な追加 の双方向)。
- 結論: 蒸留は忠実。ゲート G1–G9・§7 蒸留 N1–N6/S1/S3/S5 は行き先どおり反映。却下済み旧概念(P5・旧 masking G)の残骸なし。N6/S4 は gemma-prompt/review-server へ意図的に routing(薄さ方針として正)。
- ドリフト1件(逆方向・docs が遅れ): SKILL.md は task-handoff を composition に持つが docs/方法論 §6 の composed-skills に欠落。→ §6 に「ブランチ点の合成=task-handoff(2026-06-22 起こし済)」の一行を追加して解消。
- 不採用(意図的に直さない): ①「Every turn」フェーズ列が Scaffold/Pre-P0.0/GT-established を省く=phase routing に全部あり実害なし・簡潔さ優先。②P3 votes ニュアンス(境界FP入れ替え/faint recall に効く)は docs 留め=ハーネス詳細ゆえ薄いオーケストレータに載せない。grill-hook「K & votes」でカバー。

## 2026-06-23 — G7 を単位依存度の gate 型問いへ書き直し(grill-me で詰めた結果)
- 発端: 「共通基盤(ディレクトリ・取り込み)は要件定義より前では」という問いから、現 G7 の内部矛盾が露見=「OTHER 横断を全部 SETTLE まで遅らせる」が、P0.0 が早期に命じる取り込み(pdf-to-md/xlsx)まで遅らせてしまう。
- 詰めの帰結(grill-me): 当初「3バケツ taxonomy を書き、別ドメインで検証」案を立てたが撤回。「何が単位依存か」は螺旋でしか分からない=紙で事前列挙する taxonomy 化は、単位を計器なしに紙で固める category error の再演(オーナー指摘「B=ロジック改善ループと同義」)。書けるのは早見表でなく gate 型の問いだけ。
- 決定: G7 を二分法(計器=早/他=遅)から「単位依存度で時を決める」へ書き直す。単位非依存(scaffold・取り込み・アクセス層)=早くてよい(出戻りなし)/単位依存=SETTLE まで/例外=診断計器は単位依存だが config 駆動で早く(再設定で吸収)。判別不能なら pre-SETTLE とみなし計器以外を遅らせる。
- なぜ今やってよいか(n=1 リスク無し): これは あかつきからの帰納でなく既存前提(単位は創発・動く土台に建てるな)からの演繹。ゆえに別ドメイン待ちで寝かせる必要がない。pressure-test 対象でもない。
- 観測変化(本丸): 次ドメインで「何を要件前に作り何を SETTLE まで待つか」の判断ミスを減らし、メタ作業の回り道を削ってロジック改善ループに時間を回す(方法論 §1)。
- 反映: SKILL.md G7 / docs/方法論 §5 G7 を両方書き直し(整合維持)。

## 2026-06-23 — review-server のログインを前倒し設計から外す(認証はモードでなくルートへ格下げ)
- 発端(オーナー): 「サーバーのパスワードは要らない。名前も要らない。爆速でサーバーを立ててロジック改善ループを回す想定だから、ある程度完成したものをファクトチェッカーが見るようになる。事前設計としてログイン画面を設けるのは over」。
- 却下した選択: 「二役 = ログイン(DEV_PASSWORD + cookie セッション + reviewer 名)」を初期から実装すること。template/server.py は実際にログイン画面・パスワード・名前・セッションを持っていた。
- 切り分け(残すゲート): アンカリング・ファイアウォール(S3/S4)は方法論ゲートで非交渉。ただしその実体は「GT作成経路が ADAPTER.judges() を一切描画しない」という RENDER-TIME の構造であって認証ではない。ゆえにログインを外してもファイアウォールは無傷で残る(smoke test: /review/<key> は judge 0件、/diag/<key> は judge 表示)。オーナーはファイアウォール自体は否定していない。
- 決定: 役割を「ログイン」から「モード=ルート選択(/diag・/review、既定で認証なし)」へ格下げ。初期は開発者一人が高速ループを回し、人間 blind レビュアーは完成度が上がってから来る。reviewer 帰属は定数 anon。認証強度・名前付きレビュアーは「外部の信用できない blind レビュアー投入時(隔離/帰属が要るとき)」の後出し grill hook とし、事前にログイン画面を建てない。
- なぜ今やってよいか(n=1 リスク無し): あかつきからの帰納でなく既存前提(G7=必要になってから infra を建てる/人間 GT は遅く来る)からの演繹。pressure-test 対象でない。
- 反映: review-server SKILL.md(description / S3 / W2 / CHOICES auth strength)・template/server.py(ログイン機構を撤去しルート分岐へ)・template/README.md(Run / Modes)。判定の中身に変更なし。judge-loop SKILL.md はモード記述ベースで既に整合、変更不要。

## 2026-06-23 — G10 新設: 書いたプロンプトは必ずオーナー承認を仰ぐ(非交渉)
- 発端(オーナー): 「judge-loop で Claude がプロンプトを作成する時は、絶対に user にチェックを仰ぐように設計したい」。
- 確定した選択(grill 2問): ①対象範囲 = 全 LLM 実行プロンプト(judge prompt だけでなく中立ファクト層・各軸・eval/抽出も)。②実装箇所 = judge-loop(ゲート)＋ gemma-prompt(発生源側強制)の両方。「絶対」=ブロッキングの gate として扱う。
- 決定: ゲート G10 を新設。書いた/直した LLM 実行プロンプトは全文をオーナーに提示し、明示の承認を得てから初めてパイプラインへ投入(使用/配線/コミット)。Claude は自己承認も黙った投入もしない。何をどう変えたかと理由を添える。提示はプロンプト本文そのもの(正解/期待出力を埋め込まない=eval汚染防止・global「Writing prompts for LLM executors」②と整合)。
- なぜゲートにしてよいか(n=1 リスク無し): あかつき帰納でなく既存前提(オーナー=審級・人間ループが連続 back-stage・G3 報酬はオーナー反応)からの演繹。ゆえに別ドメイン待ちで寝かせず、pressure-test 対象でもない(G7/ログイン格下げと同じ演繹系)。
- 既存ゲートとの関係: G3(judge を報酬にしない=出力の承認)と G10(プロンプト自体の承認)は別レイヤ。G6(却下/裁定を台帳へ)とも別=G10 は投入前の事前承認。
- 反映: judge-loop SKILL.md(Gates に G10 / Gates vs choices の G1–G10 / Composition の gemma-prompt 行に sign-off 明記)・gemma-prompt SKILL.md(7か条の7項目めに owner sign-off を追加=従来6項目だったのを補完)・docs/方法論 §5 横断ゲートに G10 / D5 と §6 deferred の G1-G9→G1-G10。判定の中身に変更なし。

## 2026-06-24 — S1 のスコープ訂正: 乱立は開発者側・ファクトチェッカー面は別成果物に分離可
- 発端(オーナー): 「ファクトチェッカーに渡す時はフォルダが切り分けられる。そのまま渡すとごちゃごちゃする。別サーバーの方が良いのでは。前乱立したのは開発者用サーバーであって、ファクトチェッカー用は1つしか立てていない」。
- 一次資料で裏取り(jibun-de・review-server/docs/事例 1〜2章): 開発者側=9断片+削除済4で完全な乱立(F1→S1 の出典は全て検証アングル:横断/keydiff/flutter/checklist/形式/乗換/禁止)。人間GT作成側=factcheck/server.py ただ1つ(最成熟)。review_server.py は使い捨て先行物、labels.json 2回廃棄は GT 廃棄(S5/S6)でサーバ乱立ではない。→ オーナーの史実認識は正しい。anti-sprawl の根拠は人間面に存在しない。
- 認めた誤り(前々ターンの私): S1 を「ファクトチェッカー面も1サーバに畳め」と当てたのは過剰適用。別文脈(開発者乱立)で帰納したルールの継承=judge-loop 自身が戒める誤り。
- ただし一次資料が示す本丸(F2→S2・最重要): 最も高くついた失敗はまさに分離した人間サーバ(factcheck)で発生=契約を別に焼き直してドリフト(廃止語再導入・旧フィールド名で全件空表示=レビュー不能)。教訓は「分けるな」でなく「契約を複写するな」。サーバが2つあること自体は罪でない。
- 決定: ①S1 のスコープを開発者診断面に限定。②ファクトチェッカーGT作成面は引き渡し可能な別成果物(別フォルダ/パッケージ/サーバ)として分離可、むしろ引き渡し衛生と防火壁を強化。③跨いで守る不変=S2 単一契約ソース(別成果物の契約は単一ソースから生成・手書き禁止)・S9 GT戻し1経路(inbox CSV)・S6 provenance/tier。④防火壁の最強形=不在による隠蔽(別パッケージに機械の答えが入らない・S10 除外)。前ターンの auth 保留弱点(reviewer が /diag を叩けば見える)はこれで構造ごと解消=上書き。
- なぜ今やってよいか(n=1 リスク無し): 一次資料(あかつき史実)に直接接地した訂正であり、別ドメイン待ちでない。判定の中身には触れない設計記述のみ。
- 不採用: 「今ファクトチェッカー別サーバ/別スキルを立てる」=G7 で premature(オーナーも後回しでよいと明言)。方法論訂正のみ行い、起こしは TODO Deferred(トリガ=外部FC実投入)。
- 反映: review-server SKILL.md(冒頭3モード記述 / S1 スコープ / S2 生成契約 / S3 不在防火壁 / CHOICES に「FC面=モードか別成果物か」)・docs/事例 3章(1サーバ2ロール理想形→1開発サーバ+1人間成果物・契約単一へ訂正)・judge-loop SKILL.md(P2 / Composition の「ONE server」緩和)。TODO Deferred に切り出し候補を起こす。

## 2026-06-24 — レビュー面の form factor: 診断=IDEエルゴ / blind=最小(非IDE)
- 発端(オーナー): 「人間が確認する時、基本 IDE 形式が理想かも」。
- 詰めの帰結: form factor はモードで割れる。診断面はメンタルモデルがコードレビューそのもの(多数ユニットを送る・evidence=go-to-definition・divergence=Problems・A/A·A/B·flutter=diff)→ IDE エルゴ(3ペイン+キーバインド+diff+Problemsパネル)が理想。blind GT作成面は逆=ファイアウォールが「情報の貧しさ」を要求するので意図的に最小(IDE の show-everything は blindness と戦う)。偶然でなく、ファイアウォールと観客が両モードを反対方向へ引く。
- 決定: 診断面は IDE エルゴ寄せ可・blind は最小のまま。本物の IDE 統合(単位=開発者エディタのファイル)は別 deferred=単位 settle まで建てない(G7)。
- なぜゲートでなく選択/方向か: 観客と防火壁から演繹した設計方向であって不変条件ではない。焼き込まず方向として残す。
- 反映: review-server SKILL.md W2(form factor 一行)・judge-loop TODO Deferred(本物のIDE統合・トリガ=単位settle&人間GT量)。

## 2026-06-24 — 自律度軸 L1→L2→L3 を新設(設計確定・task-handoff で fan out)
- 発端(オーナー): 10セッション並列運用(人間=聖徳太子化)。judge-loop に「ハンドルを誰が握るか」=自律度の軸が無い。フェーズ軸しか持っていなかった。
- 確定: フェーズ軸に直交する二次元目。ペーシング(各ステップを人間が律速)と審級(最終的に何が正しいかを人間が裁く)を分離。自律度軸は人間をペーシングから外すが審級からは外さない(G3/G10 不変)。
  - L1 人間ペース(現状)。L2 自走+claude×gemma×claude 裁定(裁定者バイアス→外部アンカー照合で回避・gemma=脱相関)。L3 自己起案=コンサル(逆向き要件定義・no-GT 限定・提案のみ)。
  - 依存連鎖: L1 が人間コミットでアンカー→L2 の裁定基準→L2 の override が L3 の採掘対象。
- L3 コンサルの核心(grill で潰した順): ①コンテキスト無関係 → ②priors free / evidence bound(データ読まないコンサル=占い)→ ③境界認識・steerable(要件を知るからこそ外を探索・距離はメインが振る)→ ④探索と製錬の分離=「原石はノイズからしか生まれない」(探索を刈るな=G9 街灯バイアスの生成側・製錬は内部・出口だけ固定=提案軸+動機ケースの対)。
- 案A(標準を書く)を採用・案B(動くコンサルを今 build)を却下: judge-loop は標準であって実装でない。build 系は TODO Deferred(overfit リスク)。gem-from-noise は閉じた仕様で書けない=書けるのは規律だけ=標準の一段として書くのが正しい。
- L3 は別スキルへ routing=薄さ堅持。「task-handoff で渡した後にコンサルとして自走する」ためプロンプト化が必要(オーナー指示)。
- 命名+スコープ昇格(同日オーナー): スキル名 `consultant`(prospect・konsaru-tan/コンサルたん案は却下、素直な consultant に確定)。かつ「単独でも役に立つ」→ judge-loop L3 専用部品でなく独立スキルへ昇格。judge-loop は composed-by(review-server/gemma-prompt と同位)、誰でも直呼び可。handoff 成果物を反映済(foundation/両 TASK/decision/registry)。
- 発行物: `_dev/handoff/`(foundation + 2兄弟タスク[autonomy-axis / consultant-skill] + 各 decision seed + registry)。共有 foundation 版 `judge-loop@ac4e3fb · SKILL.md blob e0b94af`。両ストリームは cold-restart 可・並列走行。回収時にここへ妥当性判定を記帳する。
- grill-hook 却下(蒸し返し防止): 「全くコンテキスト無関係なコンサル」却下=占いに堕ちる。「探索を効率で刈る」却下=街灯バイアス。「生フラグ羅列だけ/生軸提案だけ」却下=コンサルでない。
