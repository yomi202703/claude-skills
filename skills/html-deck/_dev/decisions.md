# html-deck decisions (append-only ADR)

新しい項目は下に足す。過去エントリは書き換えない。

## 2026-06-27 初版コミット + 推奨雛形を同梱

- html-deck を初版コミット(SKILL.md + scripts/gen_template.py + templates/template.html + templates/antipattern.html)。
- スキルは「ジェネレータ」ではなく「作法(方法論)」。判定・スコアリング・一覧の単一HTMLを作る/レビューする規律。

## 2026-06-27 「badのデザインが好き」→ 削るのは情報設計であって見た目ではない

- ユーザーがアンチパターン版(濃色ヘッダー・罫線・角丸・チップ)のリッチな見た目を好んだ。
- 決定: このスキルが削るのは情報設計(死に列・verdictラベルの二重持ち・免責注釈・悪い並び)であって視覚的装飾ではない。装飾は自由と SKILL.md「雛形」節に明記。
- 帰結: template.html を「リッチな見た目＋絞った情報」の推奨雛形として確立(=styled版)。antipattern.html を対の悪い版として review 参照に残す。

## 2026-06-27 3者レビュー(ChatGPT / Claude Desktop / Antigravity)を受けた原則改訂

- 外部3エージェントに現状ブリーフを投げて批評を求めた。Antigravity は駆動できたが応答キャプチャに3回失敗、ChatGPT と Claude Desktop の2者で確定。
- 2者が独立に一致した急所と、それへの対応:
  - 色への一本足: 原則2が判断を着色だけに載せ、@media print を謳いながらモノクロ・色覚で判断が消える自己矛盾。→ 原則2に二重符号化(太字・左ライン・位置)を必須化。雛形は既に準拠(太字＋左赤ライン＋背景色)だったので、当たっていたのは出荷物でなく原則テキスト。文言のみ改訂。
  - 閾値・分母の出所が消える/「免責注釈=失敗」は行き過ぎ(規制領域では法定注釈が必須): → メタ原則に例外を追加。(a)分母・期間・閾値根拠 (b)法定・契約上の開示 は守りの注釈でなく構造情報、消さない。
  - 「削る=普遍善」が一番危うい前提(2者一致): 削りは送り手の自信であって受け手の理解ではない。→ メタ原則に歯止めを一行(受け手の理解が上がるときだけ削る/万能デッキ前提を疑う)。
  - 最小 eval が無い・例が1系統だけ(自己採点): → TODO に回す(本コミットでは未対応)。
- 私(統合役)の補正: 「色一本足」批判はコードを読めば雛形に当たらない。レビュー陣はソースを読んでいない分そこを過大評価していた、と記録。
- 採用方針(ユーザー「推奨を採用」): P0(原則2・メタ原則の文言)を SKILL.md へ即反映、P1(削る=善の歯止め)も同じメタ原則節に同梱、P2(eval/4系統の例)は本 _dev に記録して TODO へ。

## 2026-06-27 適合性判定.html を判定表系統の正準サンプルに採用 + 3つの型を昇格

- ユーザー所有の ~/Downloads/適合性判定.html を「MTGに出せる」見本として精読。template.html の上位互換で、原則をほぼ全て満たし、かつスキル未言語化の型を3つ持つと判断。
- 匿名化(scripts/anonymize.py 相当の一回変換)して templates/sample_judgment_table.html として同梱: 社名 あかつき→サンプル証券、口座番号49件を全テーブル横断で 9xxxxx に一貫置換(prose言及も追従)、localhost リンク除去。元データの数値・構造は保持。
- SKILL.md に「判定表の型」節と対応するチェックリスト3行を追加:
  - funnel を背骨に(結論の見出しを大きな数字で先に。全件表は監査証跡)。レビュー陣の「削るだけでは足りない/次の行動が一意に近づく構造」への具体回答。
  - 閾値を単位ごとに可変(投資方針別の線)。一律閾値で保守側が埋もれるのを防ぐ。今日の「効かない列は相手依存」という前提と整合。
  - 判定保留を偽verdict無しで最下段＋理由。限界を相手のアクションに翻訳(免責で終わらせない)。
- TODO P1 を更新: 判定表系統は充足、残りスコア表・抽出一覧・リスク一覧の3系統へ。
